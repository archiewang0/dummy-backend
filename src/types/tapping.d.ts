declare namespace Jet {
    interface ITappingPrime {
        mobile: string;
        prime: string;
        amount: number;
        currency?: string
        order_number: string;
        transaction_id: string;
    }
}